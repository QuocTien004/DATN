# World Model + MetaDrive dependencies
#
# PyTorch: cài RIÊNG trong venv (chọn 1 dòng dưới), rồi mới:
#   pip install -r requirements.txt
#
# GPU (CUDA 12.4) — khuyến nghị nếu có NVIDIA (vd. GTX 1650):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#
# CPU only:
#   pip install torch torchvision torchaudio
#
# Không pin torch ở dưới để tránh pip kéo nhầm bản CPU từ PyPI.

metadrive-simulator>=0.4.3
numpy>=1.24.0
matplotlib>=3.7.0
jupyter>=1.0.0
wandb>=0.16.0
pyyaml>=6.0
gymnasium>=0.29.0
tqdm>=4.66.0
